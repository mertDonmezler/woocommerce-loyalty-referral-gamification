name: Gorilla Loyalty & Gamification

on:
  push:
    paths:
      - 'gorilla-loyalty-gamification/**'
    tags:
      - 'loyalty-v*'
  pull_request:
    paths:
      - 'gorilla-loyalty-gamification/**'

jobs:
  lint:
    name: PHP Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: phpcs

      - name: PHP Syntax Check
        run: find gorilla-loyalty-gamification -name "*.php" -exec php -l {} \;

      - name: PHPCS (Advisory)
        continue-on-error: true
        run: |
          phpcs --standard=WordPress-Core --extensions=php \
            --ignore=*/vendor/*,*/node_modules/* \
            --report=summary \
            gorilla-loyalty-gamification/

  build:
    name: Build ZIP
    needs: lint
    if: startsWith(github.ref, 'refs/tags/loyalty-v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/loyalty-v}" >> $GITHUB_OUTPUT

      - name: Create ZIP
        run: |
          cd gorilla-loyalty-gamification
          zip -r ../gorilla-loyalty-gamification-v${{ steps.version.outputs.version }}.zip . \
            -x "*.git*" "*.DS_Store" "*__MACOSX*"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gorilla-loyalty-gamification-v${{ steps.version.outputs.version }}
          path: gorilla-loyalty-gamification-v${{ steps.version.outputs.version }}.zip

  release:
    name: GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/loyalty-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/loyalty-v}" >> $GITHUB_OUTPUT

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: gorilla-loyalty-gamification-v${{ steps.version.outputs.version }}

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Gorilla Loyalty & Gamification v${{ steps.version.outputs.version }}"
          body: |
            ## Gorilla Loyalty & Gamification v${{ steps.version.outputs.version }}

            ### Installation
            1. Download the ZIP file below
            2. Go to WordPress Admin > Plugins > Add New > Upload Plugin
            3. Upload the ZIP file and activate

            ### Requirements
            - WordPress 6.0+
            - WooCommerce 7.0+
            - PHP 7.4+
          files: gorilla-loyalty-gamification-v${{ steps.version.outputs.version }}.zip
